import { FC } from 'react'
import styles from './styles.module.css'

type TProps = {}

export const End: FC<TProps> = ({  }) => {
  return (
    <div className={styles.end}>
      End
    </div>
  )
}